spring:
  application:
    name: log-service

  cloud:
    config:
      profile: native
      uri: http://localhost:8888

eureka:
  client:
    service-url:
      defaultZone : http://localhost:8500/eureka/
